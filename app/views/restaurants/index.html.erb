<h1>RESTAURANTS LIST</h1>

<% @restaurants.each do |restaurant| %>
<p># <%= link_to restaurant.name, restaurant_path(restaurant.id)%></p>
<% end %>
<%= link_to new_restaurant_path do %>
  <i class="fa fa-plus">Add a new restaurant</i>
<% end %>